// musicController.test.js
const { uploadMusic, getAllMusicForUser, getPublicMusicByUser } = require('../controllers/musicController');

describe('Testing music functions...', () => {
    it('', () => {

    });
});